<% layout('layouts/boilerplate') %>
<div class="container mt-4">
  <h2>Listing Analytics: <%= listing.title %></h2>
  <div class="row g-3">
    <div class="col-md-3"><div class="card p-3"><div class="fw-bold">Views</div><div><%= (stats&&stats.viewsCount)||0 %></div></div></div>
    <div class="col-md-3"><div class="card p-3"><div class="fw-bold">Bookings</div><div><%= (stats&&stats.bookingsCount)||0 %></div></div></div>
    <div class="col-md-3"><div class="card p-3"><div class="fw-bold">Revenue</div><div>₹<%= (stats&&stats.revenue)||0 %></div></div></div>
    <div class="col-md-3"><div class="card p-3"><div class="fw-bold">Avg Rating</div><div><%= ((stats&&stats.averageRating)||0).toFixed(2) %> (<%= (stats&&stats.reviewsCount)||0 %>)</div></div></div>
  </div>
</div>


